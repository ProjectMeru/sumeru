<?xml version="1.0" encoding="utf-8"?>
<sumeru>
    <data>
        <!-- sys.view : core.user (kanban) -->
        <view id="view_core.user_kanban" model="core.user" type="kanban">
            <field name="color" invisible="true"/>
            <field name="gender" invisible="true"/>
            <field name="image"/>
            <field name="name" string="Name"/>
            <field name="login" string="Login"/>
            <field name="email" string="Email"/>
        </view>
    </data>
</sumeru>
